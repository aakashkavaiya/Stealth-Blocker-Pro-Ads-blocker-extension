[package]
name = "stealth_blocker_host"
version = "1.0.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full", "io-util"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
trust-dns-resolver = "0.22"
